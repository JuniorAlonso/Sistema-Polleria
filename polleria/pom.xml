<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- =====================================================
         PARENT: Spring Boot BOM — hereda a todos los módulos
    ====================================================== -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.5</version>
        <relativePath />
    </parent>

    <!-- =====================================================
         IDENTIFICADOR DEL PROYECTO PADRE
    ====================================================== -->
    <groupId>com.sistema.polleria</groupId>
    <artifactId>polleria</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>Sistema Pollería</name>
    <description>Plataforma de pedidos y delivery para pollería — Arquitectura de Microservicios</description>

    <!-- =====================================================
         MÓDULOS — cada microservicio es un submódulo
    ====================================================== -->
    <modules>
        <module>auth-service</module>
        <module>orders-service</module>
        <module>payments-service</module>
        <!-- Próximos módulos:
        <module>inventory-service</module>
        <module>finance-service</module>
        <module>whatsapp-service</module>
        <module>chatbot-service</module>
        -->
    </modules>

    <!-- =====================================================
         PROPIEDADES COMPARTIDAS
    ====================================================== -->
    <properties>
        <java.version>21</java.version>
        <jjwt.version>0.12.6</jjwt.version>
        <lombok.version>1.18.32</lombok.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <!-- =====================================================
         GESTIÓN DE DEPENDENCIAS COMPARTIDAS
         Los hijos declaran la dependencia SIN versión — la
         heredan desde aquí. Evita inconsistencias entre servicios.
    ====================================================== -->
    <dependencyManagement>
        <dependencies>
            <!-- Lombok -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- JWT -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- =====================================================
         BUILD — plugins compartidos para todos los módulos
    ====================================================== -->
    <build>
        <pluginManagement>
            <plugins>
                <!-- Lombok como annotation processor explícito para Java 21 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>